import React from 'react'
import { Divider } from 'react-native-elements'

export default function CloseBar() {
  return <Divider style={{marginLeft:140, marginRight:140, borderRadius:20}} width={5} color={'#c0c0c0'} />
}